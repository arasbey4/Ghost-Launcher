import { PlaceholderPage } from '@/components/placeholder/PlaceholderPage'

export function CloudPage(): JSX.Element {
  return (
    <PlaceholderPage
      title="Cloud"
      icon="☁️"
      description="Sync your saves, settings and library across all your devices."
    />
  )
}
